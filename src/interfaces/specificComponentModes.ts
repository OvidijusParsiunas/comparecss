// import { COMPONENT_MODES } from '../consts/componentModes.enum';
// import { ComponentModes } from './componentModes';

// // extends keyof - allows generic value to be used for keyof values
// export type SpecificComponentModes<T extends keyof ComponentModes> = {
//   [key in keyof Required<Pick<ComponentModes, T>>]: COMPONENT_MODES;
// }
